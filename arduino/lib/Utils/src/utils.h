#ifndef UTILS_H
#define UTILS_H

/**
 * @brief Checks if a number is between some boundaries.
 *
 * @param n The number to bind
 * @param min The lower bound (inclusive)
 * @param max The upper bound (inclusive)
 * @return bool Returns true if the number is between min and max
 */
bool inBounds(int n, int min, int max);

#endif